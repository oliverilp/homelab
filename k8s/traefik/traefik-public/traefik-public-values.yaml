service:
  type: LoadBalancer
  spec:
    loadBalancerIP: 10.1.20.50

providers:
  kubernetesCRD:
    enabled: true
  kubernetesGateway:
    enabled: true
    # Both GatewayClasses share controllerName traefik.io/gateway-controller; without this each
    # instance reconciles both Gateways, serves the other's routes, and flaps status.addresses
    labelSelector: traefik-instance=public

additionalArguments:
  - "--entryPoints.web.http.middlewares=traefik-error-pages@kubernetescrd"
  - "--entryPoints.websecure.http.middlewares=traefik-error-pages@kubernetescrd"

ports:
  websecure:
    http3:
      enabled: false

gateway:
  enabled: false

gatewayClass:
  enabled: true
  name: traefik-public
  labels:
    traefik-instance: public

ingressClass:
  enabled: false
  isDefaultClass: false

metrics:
  prometheus:
    enabled: true
    service:
      labels:
        gateway-type: public

deployment:
  replicas: 2
  podLabels:
    gateway-type: public
    traefik-instance: public

resources:
  requests:
    cpu: 350m
    memory: 180Mi
  limits:
    memory: 180Mi

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: node-role.kubernetes.io/control-plane
              operator: DoesNotExist
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: traefik
              traefik-instance: public
          topologyKey: kubernetes.io/hostname
