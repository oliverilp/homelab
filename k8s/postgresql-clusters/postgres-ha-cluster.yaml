apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: postgresql
spec:
  imageName: ghcr.io/tensorchord/cloudnative-pgvecto.rs:17.0@sha256:3e2052d40b892dcd6b356ee95d93184b881b708b86d7034bf6a1be56ce86be1f
  instances: 3
  
  managed:
    roles:
    - name: memos
      ensure: present
      comment: "Memos application user"
      login: true
      superuser: false
      createdb: false
      createrole: false
      passwordSecret:
        name: memos-postgres-credentials
    - name: linkwarden
      ensure: present
      comment: "Linkwarden application user"
      login: true
      superuser: false
      createdb: false
      createrole: false
      passwordSecret:
        name: linkwarden-postgres-credentials
    - name: immich
      ensure: present
      comment: "Immich application user"
      login: true
      superuser: false
      createdb: false
      createrole: false
      passwordSecret:
        name: immich-postgres-credentials

  bootstrap:
    initdb:
      database: app
      owner: app
      secret:
        name: postgres-credentials

  superuserSecret:
    name: postgres-superuser-credentials

  storage:
    size: 10Gi
    storageClass: nfs-app-data

  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "1Gi"  
      cpu: "1000m"

  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: node-role.kubernetes.io/control-plane
            operator: DoesNotExist
    podAntiAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
      - labelSelector:
          matchLabels:
            cnpg.io/cluster: postgres
        topologyKey: kubernetes.io/hostname

  monitoring:
    enabled: true 
