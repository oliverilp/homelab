apiVersion: batch/v1
kind: CronJob
metadata:
  name: mkv-strip
  namespace: mkv-strip
  labels:
    app: mkv-strip
spec:
  schedule: "30 0 * * *"
  timeZone: Europe/Tallinn
  concurrencyPolicy: Forbid
  startingDeadlineSeconds: 3600
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            app: mkv-strip
        spec:
          affinity:
            nodeAffinity:
              preferredDuringSchedulingIgnoredDuringExecution:
                - weight: 100
                  preference:
                    matchExpressions:
                      - key: kubernetes.io/hostname
                        operator: In
                        values:
                          - k8s-worker-01
          automountServiceAccountToken: false
          enableServiceLinks: false
          hostIPC: false
          hostNetwork: false
          hostPID: false
          restartPolicy: Never
          shareProcessNamespace: false
          terminationGracePeriodSeconds: 60
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            fsGroup: 0
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: mkv-strip
              image: ghcr.io/oliverilp/mkv-strip:2026.4.26@sha256:912346b08264a210717cdb096f0d2355a7c92eb87297b2ceaa32b1267edecb47
              imagePullPolicy: IfNotPresent
              env:
                - name: TZ
                  value: Europe/Tallinn
                - name: MKV_STRIP_CONFIG
                  value: /etc/mkv-strip/config.json
              resources:
                requests:
                  cpu: 500m
                  memory: 256Mi
                limits:
                  cpu: "2"
                  memory: 512Mi
              securityContext:
                allowPrivilegeEscalation: false
                privileged: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop:
                    - ALL
                  add:
                    - DAC_OVERRIDE
              volumeMounts:
                - name: config
                  mountPath: /etc/mkv-strip
                  readOnly: true
                - name: media
                  mountPath: /media
                - name: tmp
                  mountPath: /tmp
          volumes:
            - name: config
              configMap:
                name: mkv-strip-config
            - name: media
              persistentVolumeClaim:
                claimName: mkv-strip-media-pvc
            - name: tmp
              emptyDir: {}
