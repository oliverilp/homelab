apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filestash-data-pvc
  namespace: filestash
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-app-data
  resources:
    requests:
      storage: 5Gi
