apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memos-data-pvc
  namespace: memos
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 2Gi 
