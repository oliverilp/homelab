apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zigbee2mqtt
  namespace: homeassistant
  labels:
    app: zigbee2mqtt
spec:
  serviceName: zigbee2mqtt
  replicas: 1
  selector:
    matchLabels:
      app: zigbee2mqtt
  template:
    metadata:
      labels:
        app: zigbee2mqtt
    spec:
      initContainers:
        - name: config-init
          image: busybox:1.37
          command: ["sh", "-c"]
          args:
            - |
              # Copy config from ConfigMap to writable location if it doesn't exist
              if [ ! -f /app/data/configuration.yaml ]; then
                cp /tmp/config/configuration.yaml /app/data/configuration.yaml
                echo "Configuration copied from ConfigMap"
              else
                echo "Configuration already exists, skipping copy"
              fi
          volumeMounts:
            - name: zigbee2mqtt-data
              mountPath: /app/data
            - name: zigbee2mqtt-config
              mountPath: /tmp/config
              readOnly: true
      containers:
        - name: zigbee2mqtt
          image: koenkk/zigbee2mqtt:2.8.0
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: TZ
              value: "Europe/Tallinn"
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
          volumeMounts:
            - name: zigbee2mqtt-data
              mountPath: /app/data
      volumes:
        - name: zigbee2mqtt-data
          persistentVolumeClaim:
            claimName: zigbee2mqtt-data-pvc
        - name: zigbee2mqtt-config
          configMap:
            name: zigbee2mqtt-config
