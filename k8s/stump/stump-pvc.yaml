apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stump-data-pvc
  namespace: stump
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
